# 2026-08-12 — `202303251679660111823147.pdf` 解析卡住根因与 `ShapeRecognizer` 性能修复

## 任务背景

用户反馈：使用 `org.opendataloader.pdf.DebugSample` 解析

```text
D:\Code\JavaCode\opendataloader-pdf-parse\opendataloader-pdf\docs\pdf\202303251679660111823147.pdf
```

时进程卡住，需要查明原因和解决方案。`DebugSample` 已配置为该文件，且用户要求修改代码前必须获得同意。

## 复现过程

直接运行 `DebugSample`，日志到第 2 页形状识别（`ShapeRecognizer.recognize`）后不再前进，CPU 满载。

```bash
cd java/opendataloader-pdf-core
CP=$(cat cp.txt)
timeout 120 java -cp "target/classes;${CP}" org.opendataloader.pdf.DebugSample
```

输出停在：

```text
org.opendataloader.pdf.processors.ShapeRecognizer recognize
信息: Page 2: recognized 1 shape(s)
org.opendataloader.pdf.processors.ShapeRecognizer recognize
信息: Page 2: shape type=rectangle, components=1, bbox=[0.0, 0.0, 595.28, 807.87]
```

之后 120 秒超时进程被强制终止。

## 根因定位

### 第一步：线程 dump

后台启动进程，30 秒后 `jstack` 抓取主线程栈：

```text
"main" #1 ... RUNNABLE
    at java.util.LinkedList.node(LinkedList.java:575)
    at java.util.LinkedList.get(LinkedList.java:481)
    at org.opendataloader.pdf.processors.ShapeRecognizer.buildChains(ShapeRecognizer.java:870)
    at org.opendataloader.pdf.processors.ShapeRecognizer.recognizePolylines(ShapeRecognizer.java:414)
    at org.opendataloader.pdf.processors.ShapeRecognizer.recognizePage(ShapeRecognizer.java:256)
```

连续两次间隔 30 秒的 dump 都停在同一行，且 CPU 时间持续增长（255 秒 CPU / 255 秒 wall），说明不是死锁，而是**在热循环里反复遍历**。

### 第二步：对象数量确认

`jmap -histo:live` 显示现场对象规模：

| 类 | 实例数 |
|---|---|
| `org.verapdf.wcag.algorithms.entities.content.LineChunk` | 220,984 |
| `org.verapdf.wcag.algorithms.entities.geometry.Vertex` | 441,968 |
| `java.util.LinkedList` | 1,452,314 |

该 PDF 的某一页产生了超过 22 万条线段。

### 第三步：代码分析

`ShapeRecognizer.buildChains` 的实现：

```java
List<LineChunk> remaining = new LinkedList<>(lines);
...
for (int i = 0; i < remaining.size(); i++) {
    LineChunk candidate = remaining.get(i);   // 第 870 行
    ...
    remaining.remove(i);
}
```

- `LinkedList.get(i)` 和 `remove(i)` 都是 **O(n)**。
- 外层还有 `while (!remaining.isEmpty())` 扫描整个列表。
- 整体复杂度接近 **O(n³)**。

当 `n` 达到 20 万量级时，运算量爆炸，进程表现为“卡住”。

## 修复策略

与用户讨论后，采用**方案 A**：

1. **先修 `buildChains` 的性能**：把 `LinkedList` 的索引访问改成 `ListIterator`，让扫描和删除都变为 O(1)。
2. **加阈值兜底**：单个颜色的 line 数量超过常量 `5000` 时，跳过该颜色的 polyline / connector 识别，并打印 warning。Filled rectangle / bar chart 识别不受影响。

### 为什么阈值只作用于 polyline / connector？

真正慢的是“把线段连成折线”这一步；Filled rectangle 的识别路径不走 `buildChains`。因此阈值只加在 `recognizePolylines` 和 `recognizeConnectorLines` 上，避免误伤本来能正常识别的色块/柱状图。

## 改动清单

### `java/opendataloader-pdf-core/src/main/java/org/opendataloader/pdf/processors/ShapeRecognizer.java`

#### 1. 新增 import

```java
import java.util.ListIterator;
```

#### 2. 新增阈值常量

```java
/**
 * Maximum number of line segments of a single color that will be fed into
 * chain building. Pages with extremely dense vector art (e.g. patterns or
 * highly fragmented strokes) can produce tens of thousands of segments;
 * trying to connect all of them is prohibitively expensive and rarely
 * produces useful chart shapes. Colors exceeding this limit are skipped
 * for polyline/connector recognition only.
 */
private static final int MAX_LINES_PER_COLOR_FOR_CHAIN_RECOGNITION = 5000;
```

#### 3. `buildChains` 改为 `ListIterator`

```java
private static List<List<LineChunk>> buildChains(List<LineChunk> lines) {
    LinkedList<LineChunk> remaining = new LinkedList<>(lines);
    List<List<LineChunk>> chains = new ArrayList<>();

    while (!remaining.isEmpty()) {
        LineChunk seed = remaining.removeFirst();
        List<LineChunk> chain = new ArrayList<>();
        chain.add(seed);
        Vertex start = seed.getStart();
        Vertex end = seed.getEnd();

        boolean extended = true;
        while (extended && !remaining.isEmpty()) {
            extended = false;
            ListIterator<LineChunk> it = remaining.listIterator();
            while (it.hasNext()) {
                LineChunk candidate = it.next();
                Vertex cStart = candidate.getStart();
                Vertex cEnd = candidate.getEnd();
                if (Vertex.areCloseVertexes(end, cStart, ADJACENCY_GAP)) {
                    it.remove();
                    chain.add(candidate);
                    end = cEnd;
                    extended = true;
                    break;
                } else if (Vertex.areCloseVertexes(end, cEnd, ADJACENCY_GAP)) {
                    it.remove();
                    chain.add(candidate);
                    end = cStart;
                    extended = true;
                    break;
                } else if (Vertex.areCloseVertexes(start, cEnd, ADJACENCY_GAP)) {
                    it.remove();
                    chain.add(0, candidate);
                    start = cStart;
                    extended = true;
                    break;
                } else if (Vertex.areCloseVertexes(start, cStart, ADJACENCY_GAP)) {
                    it.remove();
                    chain.add(0, candidate);
                    start = cEnd;
                    extended = true;
                    break;
                }
            }
        }
        chains.add(chain);
    }
    return chains;
}
```

#### 4. 新增 `shouldBuildChains` 守卫

```java
private static boolean shouldBuildChains(List<LineChunk> sameColorLines, int pageNumber, String kind) {
    if (sameColorLines.size() <= MAX_LINES_PER_COLOR_FOR_CHAIN_RECOGNITION) {
        return true;
    }
    LOGGER.log(Level.WARNING,
            "Page {0}: skipping {1} recognition for color with {2} line segments (threshold {3})",
            new Object[]{pageNumber + 1, kind, sameColorLines.size(),
                    MAX_LINES_PER_COLOR_FOR_CHAIN_RECOGNITION});
    return false;
}
```

#### 5. 在 `recognizePolylines` / `recognizeConnectorLines` 中调用守卫

```java
for (List<LineChunk> sameColorLines : byColor.values()) {
    if (!shouldBuildChains(sameColorLines, pageNumber, "polyline")) {
        continue;
    }
    List<List<LineChunk>> chains = buildChains(sameColorLines);
    ...
}
```

connector 分支使用 `"connector"` 作为 `kind`。

## 关键决策

### `LinkedList` 保留，但不再用索引访问

虽然 `ArrayList` 的 `get(i)` 是 O(1)，但中间删除仍是 O(n)。`LinkedList + ListIterator` 在“顺序扫描并删除当前元素”这个场景下是最贴合原算法的选择，且能保留原来的匹配顺序和结果。

### 阈值 5000 的含义

- 只对单个颜色生效，不是整页总 line 数。
- 仅跳过 polyline / connector，filled shapes 照常识别。
- 超过阈值时打印 warning，方便后续排查。

如果后续发现 5000 对正常文档太严或太松，可以只改常量，不影响逻辑。

## 验证结果

| 检查项 | 结果 |
|---|---|
| `mvn compile -q` | ✅ 通过 |
| `mvn test -Dtest=ShapeRecognizerTest -q` | ✅ 通过 |
| `DebugSample` 解析原 PDF | ✅ 约 46 秒完成，输出 JSON 与 OCR JSON |

运行日志中阈值生效：

```text
警告: Page 3: skipping polyline recognition for color with 155,688 line segments (threshold 5,000)
警告: Page 3: skipping connector recognition for color with 155,688 line segments (threshold 5,000)
```

最终输出：

```text
JSON URL / local path: D:\Code\JavaCode\opendataloader-pdf-parse\opendataloader-pdf\tmp_output\202303251679660111823147.json
OCR JSON local path:   D:\Code\JavaCode\opendataloader-pdf-parse\opendataloader-pdf\tmp_output\202303251679660111823147_ocr.json
```

## 相关文件

- `java/opendataloader-pdf-core/src/main/java/org/opendataloader/pdf/processors/ShapeRecognizer.java` —— 修复 `buildChains` 算法，新增阈值守卫
- `java/opendataloader-pdf-core/src/main/java/org/opendataloader/pdf/DebugSample.java` —— 调试入口（用户已配置目标 PDF，未改逻辑）
