# 2026-09-20

## 表格单元格内文本行合并：新增"两端非中英文"边界规则（`JsonWriter`）— 已实现

### 位置
`java/opendataloader-pdf-core/src/main/java/org/opendataloader/pdf/json/JsonWriter.java`
`writeTableBorder` 的 cell 文本装配循环（`currentCellTextLines` 累加处，约 1084-1125 行）。

### 背景（原逻辑）
单元格内每行由 `assembleGroupText` 生成，是否**新起一条**只由两个判据决定：
1. `containsEmbeddedBlock(group)` —— 图片 / 嵌套表格 HTML 独占一条；
2. `nextNewLine` —— **上一行**右端离 `cell.getRightX()` 超过 `MAX_CELL_LINE_RIGHT_GAP(15)` 就认为上一行是段末，本行新起；
   否则视为折行续行，**追加到最后一条**。

### 需求与修改
用户要求：当 **`currentText` 的首字符** 与 **`currentCellTextLines` 最后一条的末字符** *都不是* 中文（CJK 表意文字）或英文字母时，
`newLine` 也应为 `true`（即新起一条，而不是拼接）。理由：数字/标点/符号相邻时基本不可能构成同一句子的折行续行。

实现：
- 在 `boolean newLine = containsEmbeddedBlock(group) || nextNewLine;` 之后加判据，
  条件 `!newLine && !currentCellTextLines.isEmpty()`（后者保证能取到最后一条；`currentText` 此前已判空）。
- 新增私有静态方法 `isChineseOrEnglishLetter(char c)`：`[A-Za-z]` 或 CJK 统一表意文字 `\u4E00`-`\u9FFF` 返回 `true`，
  数字/标点/符号返回 `false`。（core 模块是 **source level 11**，此处无高版本语法。）

### 注意点
- 规则只作用于 `newLine == false`（即未被前两条判据命中的）情况，属于**补充收紧**，
  不会让本来该合并的"中文/英文折行"被拆开（首末字符至少一侧是中英文即不触发）。
- `else if (currentCellTextLines.isEmpty())` 分支行为未变：为空时仍直接加入首条。
- 半角字符未被纳入"英文"范围，全角标点亦不算中文；如需覆盖日文假名/韩文/CJK 扩展区，需扩展 `isChineseOrEnglishLetter`。

### 验证
`mvn -o -q -pl opendataloader-pdf-core compile` 通过（离线、PowerShell 下用 `cd '...\java'; mvn ...`）。

### 更新（同日）：全角标点纳入"中英文语境"
用户确认"全角标点需要纳入"，故：
- 判据方法由 `isChineseOrEnglishLetter` **改名**为 `isChineseOrEnglishContext`（唯一调用点同步更新），
  语义从"字母"扩为"中文/英文语境字符"：
  - `[A-Za-z]`；
  - `\u4E00`-`\u9FFF` CJK 统一表意文字；
  - `\u3000`-`\u303F` CJK 符号与标点（、。〈〉《》「」【】）；
  - `\uFF00`-`\uFFEF` 半角及全角形式（，．！？：；（）￥０-９Ａ-Ｚ）。
- 效果：两侧都是**全角标点**时不再强制新起条目（视为中文语境，允许拼接）。
- Javadoc 里用 `U+4E00` 这种写法而不是 `\u4E00`，因为 Java 会在**注释里也展开 unicode 转义**，
  写 `\u4E00` 会被渲染成汉字"一"，反而看不懂。

**已知未覆盖**（如需要再说）：中文排印常用的弯引号 `“ ” ‘ ’`、破折号 `—`/`——`、省略号 `……`
位于 General Punctuation `\u2000`-`\u206F`，**不在**上述两个块内，目前仍算"非中英文"。

### 更新（同日，第二次）：General Punctuation 弯引号/破折号/省略号也纳入
用户再次确认"需要纳入"，故在 `isChineseOrEnglishContext` 上再追加两个区间：

```java
|| (c >= '\u2010' && c <= '\u201F')   // 连字符/短横/—/― + 弯引号 ‘ ’ ‚ ‛ “ ” „ ‟
|| (c >= '\u2024' && c <= '\u2027')   // 单/双点前导符、省略号 …、连字点
```

**刻意排除** `\u2020`-`\u2023`（† ‡ • ‣）、`\u2016`-`\u2017` 之外的其余 General Punctuation
（U+2030 起：‰ ′ ″ ‹ › 等）——前者是条目符号/项目符号，属"块首标记"而非行内续行字符；
如需 `‰`（财务文档常见）或 `‹ ›`，再单独加区间。

判据最终形态（`JsonWriter.isChineseOrEnglishContext`）：
`[A-Za-z]` + `\u4E00`-`\u9FFF` + `\u3000`-`\u303F` + `\uFF00`-`\uFFEF` + `\u2010`-`\u201F` + `\u2024`-`\u2027`。

编译验证同前：`mvn -o -q -pl opendataloader-pdf-core compile` 通过。

### 补充验证与交付归档
- 跑既有单测 `JsonWriterTableCellGroupingTest`：**1/1 绿**
  （`mvn -o -pl opendataloader-pdf-core test "-Dtest=JsonWriterTableCellGroupingTest" "-Dsurefire.failIfNoSpecifiedTests=false"`），
  证明"只收紧、不放松"这一性质在真实用例上成立。
- 已按用户要求归档正式记忆文档：
  `docs/memory/2026-09-20-JsonWriter表格单元格文本行合并新增内容边界判据.md`
  （含"判据维度盘点 → 构造反例 → 布尔方向论证 → 越界分析 → 两轮 Unicode 范围迭代"的完整定位过程，
  并显式区分"跑出来的结论"与"推演出来的结论"）。
- 本轮与 `docs/memory/2026-09-08-JsonWriter表格单元格文本行合并逻辑优化与代码review.md` 是同一条逻辑的上游/下游，
  该 09-08 文档列出的 3 条遗留风险本轮**一条都没动**（合并单元格右边界、拼接分支去重、空文本 group）。

## 归档：把 09-18 的悬挂缩进修复整理成正式记忆

按用户要求，把 `.codebuddy/memory/2026-09-18.md`（当日速记）整理并归档为
`docs/memory/2026-09-18-ParagraphProcessor悬挂缩进续行被拆成两段-根因定位与修复.md`。

归档时**回到代码核对**（速记有若干处被补全/修正）：
- 4 条判据的实际实现是 `ParagraphProcessor.isHangingIndentContinuation`（**434-477 行**），
  常量 `HANGING_INDENT_RIGHT_TOLERANCE = 5.0`（53-57 行），挂载点 **363-368 行**；
- 对齐容差来源核实为 `veraPDF-wcag-algs/.../ChunksMergeUtils.java`：`ALIGNMENT_PARAM = 0.2`（55 行）、
  `getAlignment()`（406-411 行）→ 12pt 字号容差 2.4pt，缩进 22.68pt 必然判 null；
- 镜像判据即 `ParagraphProcessor.java:382-383`（速记只给了表达式，未给行号）；
- **该改动目前仍在工作区未提交**（`git diff` = 57 insertions / 0 deletions），已写入文档遗留第 4 条。
- 新增了"为什么必须插在 `hasSeparatorBetween` 之后"的理由（分隔线优先级高于几何缩进），
  这是速记里没有、但属于设计决策、必须留档的信息。

其它核对结论：`ParagraphProcessorTest` 现为 2 个用例（`testProcessParagraphs`、
`testRightAlignmentTakesPrecedenceOverTwoLineHeuristic`），确无悬挂缩进用例；
临时入口 `DebugTmpPage.java` 确认已删除。

## catalog_page_range_start 误判为第 13 页（应为第 2 页）— 根因已定位，**未改代码**（用户要求先查因）

任务 PDF：`docs/pdf/202609031788435163969004864.pdf`（21 页）。输出 `tmp_output/202609031788435163969004864.json`
里 `catalog_page_range_start=13/end=13`、`catalog_bookmarks` 12 条全是"九月 0.218 0"这种股价表行。

### 根因（三层，`CatalogBookmarkProcessor`）

1. **行级判据过松**：`ARABIC_TOC_PATTERN = "^(.*?)[\s\.]+(\d{1,5})$"` 把"任意标题 + 空白/点串 + 行尾 1-5 位数字"
   都当目录条目，标题不受任何约束。第 13 页是「5. 股份價格」每月最高/最低价表，行如
   `九月                        0.218 0.115` → 匹配为 title=`九月  ... 0.218 0`、rawPage=`115`。
2. **页级阈值只数条数**：`JsonPageTocInfo.isTocPage()` = `tocLineCount>=3 && tocLineCount/totalLines>=0.4`。
   第 13 页 12/28=0.4286 通过；第 2 页（真目录"目 錄"，6 条：責任聲明 ii / 釋義 1 / 董事會函件 3 /
   附錄一 8 / 附錄二 12 / 附錄三 15）6/8=0.75 也通过。
3. **区间择优只看命中条数**：`JsonPageRange.score() = totalTocLines * (1+ln(1+pageCount))`，两个候选都是单页，
   12 > 6 → 选第 13 页。同类评分在内容路径 `PageRange.score()` 完全一致（两条路径同病）。

### 证据

- 用真实代码（临时探针测试，跑完已删）回放：`[CatalogBookmark] detected catalog page range from JSON: 13-13 (1 pages, 12 toc items)`。
- 第 13 页 12 条命中的 rawPage = 115/14/134/126/118/115/11/107/105/103/093/083（10 条 > 总页数 21）。
- 复刻 Java 逻辑的诊断脚本（临时，放 `tmp_output/`）：`diag_toc_range.py`、`diag_toc_options.py`、
  `diag_toc_metrics.py`、`diag_toc_rules.py`。

### 候选修复（按 tmp_output 下 ~30 个历史 JSON 回归量化）

- **A. 行级"数据行否决"**（推荐）：标题里出现"数字 空格 数字"（`\d\s+\d`）即不算目录条目。
  在 24 个真实目录页上 **0 误杀**，在 4 个数据页上 **100% 命中**；修 3 个文档（本文档 13→2、
  `200711131781638086275024718` 24-27→2、`202504171785131172233015158` 56→none），其余全部不变。
- **B. 页码有效性**：阿拉伯页码须 ∈ [1, totalPages]。修本文档（12→2 条）与 `20250417`，但对
  `200711…`（财务数据行数字本身很小）无效；对"节选 PDF"有误杀风险。
- **C. 页级单调性闸门**：该页命中条目页码非递减比例 ≥ 0.7。真实目录页最低 0.80，数据页 ≤ 0.60，
  同样完美分离；风险是双栏目录的阅读顺序会打乱单调性。
- **D. 前置先验**：真实目录页都落在文档前 5%，误判页在 54%~95%。
- 现有 `minTocRatio=0.4` 挡不住第 13 页（0.4286 刚好过线）。

## 落地：目录页筛选条件"必须有'目录'标题"（用户指定方案 D，已实现）

用户选定：**目录条目之上必须存在一个单行段落，内容含简体/繁体"目录"，两字之间允许空格**。

实现（`CatalogBookmarkProcessor.java`）：
- 新常量 `CATALOG_HEADING_PATTERN = Pattern.compile("目\\s*[录錄]")`。不能用 `matches`，要用 `find`（"内容包含"）。
- `PageTocInfo` / `JsonPageTocInfo` 各加两个字段 `firstTocTopY`（该页最靠上的目录条目）与 `headingTopY`（该页最靠上的标题行），
  默认 `-infinity`；方法 `hasCatalogHeadingAboveFirstToc() = headingTopY > firstTocTopY`。
  topY 约定：JSON 路径用 `-y0`，内容路径用 verapdf `TextLine.getTopY()`，**越大越靠上**，两条路径一致。
- **只在"区间起始页"要求标题**（`detectRanges` / `detectJsonRanges` 里改为：不满足的页不能**新建**区间，但可以**延伸**已有区间）。
  否则多页目录会被截断：`202302281677505819604328` 的 12-15 会只剩 12。
- JSON 路径的"单行段落"用 `isSingleLineJsonItem(item)`（原始 `content` 数组长度 == 1，空行也算），
  所以埋在 11 行段落里的"目錄"不算标题。
- **没有 `Config` 开关**（用户没要求）；要放宽/关闭需改常量或判据。

回归（临时探针测试跑真实代码，跑完已删；脚本 `tmp_output/diag_toc_heading.py` 可复现）：
35 个历史 JSON 中 4 个变化：target `13-13 → 2-2(6)` ✅；
`200711131781638086275024718` `24-27 → 2-2(8)` ✅（真目录在 p2）；
`202504171785131172233015158` `56-56 → none` ✅（ESG 数据页误判消除）；
`20260507AN202606291826520711` `8-9 → none` ⚠️（其 '目錄...iv' 是 11 行段落第 2 行、且在首个条目之后，两条判据都不过）。

端到端：DebugSample 跑目标 PDF → `detected catalog page range from JSON: 2-2 (1 pages, 6 toc items)`，
JSON 中 `catalog_page_range_start/end = 2/2`，最终 `bookmarks` 选源变为 catalog（6 条真目录 + 从页面数据补的 2 组子项）。
单测：`CatalogBookmarkProcessorTest` 扩到 8 个用例（新增 4 个：无标题页不入选 / 简繁+空格 / 多行段落不算 / 标题在条目之后不算 / 续页延伸区间），
`-Dtest=*Bookmark*Test` 75 个用例全绿；core 全量单测中的既有失败（AutoTagger/EmbedImages/PageSeparator 等）是**路径写死为
旧工作区 `D:\Code\JavaCode\opendataloader-pdf\...` 导致的环境性失败**，与本次改动无关。

遗留观察：最终书签里 `責任聲明`(ii)、`釋義`(1) 的 `page_num` 仍是 1（罗马数字条目不参与正文页码解析，
`parseOriginalPageNum` 非纯数字返回 0 → `resolveCatalogBookmarkTarget` 直接 return），属既有问题，本次未动。
