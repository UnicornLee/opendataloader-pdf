# 2026-09-14 工作记录

## 继续「饼图识别 + 单页/多页差异」(分支 ocr-unification-20260820)

延续 `docs/memory/2026-09-11-饼图识别方案与单页多页差异根因.md` 的分析（那一轮只观测未改代码）。
用户本轮指令：①饼图如何识别 ②单页/多页差异原因+方案 ③先查因、提供方案、先不改代码 ④不清楚处澄清。
用户随后给出 5 点澄清答复，已据此敲定最终实现方案：

### 澄清答复 → 设计决定
1. 「必须含百分比标签/图例列」作为必要条件？ → **不必要**（纯几何判定即可，提高召回）。
2. 饼图截图边界是否同柱状图口径？ → **是**（圆盘+引导线+百分比标签+图例；不含标题/单位：/数据来源：）。
3. 被图覆盖的百分比/图例文字是否从文本流移除？ → **移除，不保留**（与柱状图一致）。
4. 本轮是否一并做方案2（改 FlowchartProcessor 门限）？ → **做**（需跑 FlowchartProcessorTest）。
5. 方案3（切断「页数→段落合并」全局耦合）是否纳入？ → **不纳入**（全局影响，需回归基线，单独排期）。

### 最终方案（本轮尚未改代码）
- 方案1：真正识别饼图。`ShapeChunk` 新增 `TYPE_PIE_CHART`；`ShapeRecognizer` 仿 `recognizeBarChartsFromFillBoxes` 新增
  `recognizePieChartsFromFillBoxes(pageFillBoxes)`（共角点法+并集≈正方形+角点≈中心，簇框≥3）；新增 `PieChartProcessor`（或把
  `BarChartProcessor` 抽通用 `ChartProcessor` 共享 `expandToChartRegion`），在 `BarChartProcessor` 后、`FlowchartProcessor` 前调用；
  `BoundingBoxGroupUtils` 增 `containsPieChart`；`FlowchartProcessor` 跳过含饼图组。`pageFillBoxes` 来自 `DocumentProcessor.extractPageFillBoxes()`。
- 方案2：把 `FlowchartProcessor.isFlowchartCluster` 的 `height>=MIN_HEIGHT(40)` 硬门限换成稳健判据（shapes≥2 && (poly+arrow)≥2 && text≥3 && height≥20pt），
  修 `mergedBox`/`screenshotBox` 的 ±1pt 差值，加面积上限/不吞正文保护，跑 `FlowchartProcessorTest`。
- 方案3：不做。

### 实现完成（用户说"开始"后落地）
代码改动（方案1+方案2，方案3未做）：
- `ShapeChunk` 新增 `TYPE_PIE_CHART`。
- `ShapeRecognizer`：新增 `recognizePieChartsFromFillBoxes(pageNumber, fillBoxes, existingShapes)`，用**共角点法**（候选填充框按角点重合聚簇，取最大簇；要求簇≥3、并集≈正方形、共角点≈并集中心、无单框占并集面积>0.6），产出 `TYPE_PIE_CHART` 的 `ShapeChunk`。在 `recognizePage` 中于 `recognizeBarChartsFromFillBoxes` 之后调用。
- `BoundingBoxGroupUtils`：新增 `containsType`/`containsPieChart`，`containsBarChart` 改用 `containsType`。
- `BarChartProcessor`：抽出通用 `processChartGroups(... chartType)`，`processBarChartGroups` 委托之；`expandToChartRegion` 增加 `chartType` 参数（跳过自身类型 shape）。饼图完全复用柱状图的区域扩张/截图逻辑。
- 新增 `PieChartProcessor.processPieChartGroups` → 调用 `BarChartProcessor.processChartGroups(..., TYPE_PIE_CHART)`。
- `DocumentProcessor` Loop4：`BarChartProcessor` → `PieChartProcessor` → `FlowchartProcessor` 顺序调用。
- `FlowchartProcessor`：跳过含饼图组（`containsPieChart`）；方案2：`MIN_HEIGHT` 40→20（消除 0.6pt 边缘翻转）、新增 `containsBodyText` 守卫（吸收到 height>24pt 的标题/段落则不判为流程图，防吞正文）；**撤销**了"用 mergedBox 重算 screenshotBox"的清理（会破坏既有测试 `mergesSubsequentGroupIntersectingScreenshotBox`，该清理非必要）。

### 验证
- `mvn -pl opendataloader-pdf-core test -Dtest=FlowchartProcessorTest,BarChartProcessorTest,ShapeRecognizerTest` → BUILD SUCCESS（37 tests），其中 FlowchartProcessorTest 此前 1 个失败（因上述截图框重算）已修复。
- 样本 `354cb7d4...-18.pdf`（第18页饼图）理论应被识别为 `pie_chart` 并截图；尚未在真实 PDF 上跑通（需 PaddleOCR 服务 192.168.1.97:8088，且体量大）。

### 后续建议
- 在真实样本/整册 PDF 上回归，确认单页与多页下饼图都能稳定产出且无被流程图误吞。
- 可考虑给 `recognizePieChartsFromFillBoxes` 补一个 `ShapeRecognizerTest` 单测（共角点法）。
- 方案3（段落合并与页数解耦）仍待单独排期。

### 实跑验证（本轮，用 DebugSample1 指向整册 PDF）
- **单页样本 `354cb7d4...-18.pdf` 验证成功**：重编译后在日志中确认
  `recognized pie chart with 10 wedges at bbox=[204.05,474.05,366.23,636.22]`（即完整圆盘 bbox）。
- **发现并修复一个关键缺陷**：原「共角点法的并集≈正方形」判据 **不成立**。实测 10 个扇形填充框全部以饼图圆心
  `(285.14,555.13)` 为**左下角**，因此它们的并集只有右上角一块（68×81），并非正方形 → 原判据恒为 false，饼图根本识别不出来。
  修复：改为**用独立的整圆填充框**（`[204.05,474.05,366.23,636.22]`，中心恰为圆心）作为饼图 bbox；新增 `findDiscBox`（中心≈共角点且近正方形）
  与 `boxAroundCorner`（无整圆框时的兜底：以共角点为心的正方形，半边长=扇形最远角点距离）。
  新增 `isValidPieDisc`/`isSquare` 取代 `isValidPieUnion`。
- **误检修复**：上述改法会额外把 3 个填充框组成的假簇（`bbox=[285.14,489.14,417.12,621.12]`）也判成饼图。新增
  `MIN_PIE_WEDGE_COUNT_WITHOUT_DISC=5`：无整圆框时簇内扇形须 ≥5 才接受（真饼图有整圆框，不受影响）。
- **整册 PDF `354cb7d4....pdf`（431页，含OCR）实跑验证成功**（`DebugSample1` 已指向整册）：
  - 日志：`Page 18: recognized pie chart with 10 wedges at bbox=[204.05,474.05,366.23,636.22]`，且
    `Page 18: shape type=pie_chart, components=10`。**仅一个饼图，无误检**；Page 18 **未被判为流程图**（全册唯一流程图在 Page 304）。
  - JSON：Page 18（`page_index:18`）产出 `item_type:"image"` 项，内容 `...354cb7d4..._images\imageFile32.png`，
    区域 `[199.05,204.70,438.56,378.77]`（= 圆盘 bbox 经 `expandToChartRegion` 外扩以纳入引导线/百分比/图例）。
    打开 `imageFile32.png` 目视确认：正是那页的饼图（含图例与百分比标签）→ **饼图被 PieChartProcessor 正常截图**。
- **环境坑（与改动无关，供后续复现参考）**：本沙箱 Maven 仓库在 `D:\Maven_Repo`。`mvn compile` 会报
  `JsonWriter.java` 找不到 `SemanticHeading.getColumns()`（Maven 编译期 classpath 解析到的 wcag-algorithms 版本偏旧），
  但**用运行时 classpath**（`dependency:build-classpath` 产物，`wcag-algorithms-1.31.33`，含该方法）用 `javac` 全量编译
  **可通过**。注意 cmd 的 `set /p` 读 classpath 会**截断到 1023 字符**（导致 veraPDF jar 丢失、报“包不存在”），
  必须改用 `for /f "usebackq delims=" %%a in ("cp.txt")`。因此**无需改 JsonWriter**，临时上转改动已回退，改动保持聚焦。

### 交付物：正式任务记忆文档
- 已在 `docs/memory/` 产出：
  `2026-09-14-饼图识别落地-共角点法并集判据失效根因与整册PDF验证.md`
  （含：目标/澄清口径、改动清单逐文件说明、**从"单页跑不出饼图"到"共角点簇并集非正方形→判据根本错误"的完整定位过程**、
  根本原因、单页+整册验证证据、环境坑、遗留项、相关文件）。并顺带修正了 `ShapeRecognizer` 中与该判据矛盾的旧 Javadoc。

## Pulsar `announcement_parse` 消费停摆（6 条不消费）— 定位记录（未改代码）

用户新任务：Pulsar 消费暂停、总有几条不消费（当前 6）。给了 Kibana / Nightingale 服务器 JMX / Nightingale Pulsar 看板与账号，Chrome 可直接操控。

### 用的入口 / 手法
- Nightingale Pulsar 看板 32（datasource=11，Prometheus 代理 `/api/n9e/proxy/11/api/v1/...`）；
- Nightingale 服务器 JMX 看板 201（datasource=14，instance=`app-0-88-prod-beijing1-hwcloud`）；
- Kibana Discover（dataview `logs-jetty-default`，含 `program`/`thread_name`/`host.ip`/`level`/`message`），
  用 Kibana 的 ES 代理直接查询更高效：`POST /api/console/proxy?path=<index>%2F_search&method=POST` + header `kbn-xsrf: true`。
  ⚠️ `program`/`host.ip` 等字段**不可聚合**（mapping 未建 keyword），只能客户端统计；
  `match_phrase` 对 `1789116433707014605_...` 这类下划线长数字 token 不匹配，需用 `wildcard`。

### 已确认事实（证据）
- 卡住的是订阅 `announcement_parse`（topic `persistent://capital/pdf_parse/announcement_parse`）：
  `back_log=6`、`unacked_messages=6`、`blocked_on_unacked=0`、`consumers_count=7`、`msg_rate_out=0`、
  `msg_rate_redeliver=0`、`last_consumed≈07:57:56Z`。
- 趋势（近 2h）：06:39Z 前 backlog=0 → 06:39 涌入 **2712** → 一路消费到 **6 后冻结**（30+ 分钟不变）。
- 消费者 = `opendataloader-pdf-server-hjs`，profile **`application-prod-hjs.yml`**（`receive_topic_name=announcement_parse`，`count=7`，`ack_timeout_seconds=60`）。
- 两个 pod：`192.168.0.88`(**app-0-88**，= prod-hjs，消费 `announcement_parse`) 与 `192.168.0.11`(prod-hjsinc，消费 `announcement_parse_increment`)。
- **pod .88 整体健康**：其 `pulsar-ocr-receive-*` 08:28 仍在正常收发，近 15 分钟 6802 条日志；
  **但 7 个主消费线程 `pulsar-receive-*` 全部静默**，最后日志时间：recv-0/6 06:39:39、recv-5 06:39:54、recv-2 06:39:40、
  recv-3 06:45:56、recv-4 07:23:06、recv-1 07:55:56（渐进式卡死）。对比 pod .11 的 7 个线程 08:26 全部活跃。
- 卡点位置（各线程最后日志）：recv-0/2/5/6 = OBS 图片上传后（`..._imageFile2.png`/`_streamtable_80.png`/`_ocr_60.png` + `client closing/closed`）；
  recv-3/4 = 一批 `Page NN text extraction is mostly garbled... falling back to full-page OCR`（页 58–75 / 593–618，大扫描件）。
- **JMX 看板 201（app-0-88）：CPU Core 8、CPU System Load ≈ 3~5%**（不是死循环烧 CPU）；
  8h 内 **Heap Used 峰值 3.86G / Max 4.29G（≈90%）、Old Gen 多次贴顶、GC 锯齿剧烈**（堆压力大）。
- 卡住时段（07:50–08:08）该服务 **0 条 ERROR/WARN** → 不是被应用异常（Exception）打断的。

### 机制结论（已由 jstack 确证 —— 修正：不是"阻塞"，而是"线程死亡"）
`PulsarService.consumeReceiveLoop` = 「单线程同步：receive → handleReceiveMessage(download→processForPulsar→send→清理) → ack」，
`receiverQueueSize(1)`；**循环只 catch `Exception`**（`handleReceiveMessage` 内部也只 catch `Exception`）。

**真正根因：6 个消费线程被"未被捕获的 `java.lang.Error`"打死（`Error` 不是 `Exception`，两处 catch 都拦不住）→ 线程终止。**
- 线程虽死，它持有的 `Consumer` 仍注册在 broker（`consumers_count=7` 不变），但**再没有任何线程对它调 `receive()`/`acknowledge()`**
  → 它手上那条消息**永久 unacked**，且该消费者槽位**永久失效**。
- 所以 **未确认数 == 死掉的线程数（当前 6）**；`ackTimeout=60s` 无效（无代码再回到那个 consumer）；`blocked_on_unacked=0`、`redeliver=0` 由此解释。
- 症状"总是有几个不消费" = 每次 Error 事件永久吃掉一个槽位，累积增长。

**jstack 铁证（`docs/debuglogs/jstack-app088-20260914_163816.txt`，2026-09-14 16:38:16，共 78 线程）**：
- 全部 `pulsar-receive-*` 只剩 **`pulsar-receive-1`(#33)**，`WAITING` 在 `ConsumerImpl.internalReceive → GrowableArrayBlockingQueue.take`（**空闲**）；
- **`pulsar-receive-0/-2/-3/-4/-5/-6` 全部不在 dump 里 → 已死亡**（线程 ID #31、#34–#38 共 6 个缺失）；
- `pulsar-ocr-receive-0..3`(#39/40/42/44) 都在 → OCR 侧正常；无 "Java-level deadlock" 段。

**是什么 Error**：应用日志查不到（`Exception in thread ...` 由 JVM 打到 **stderr**，未进 Kibana）。
JMX 看板 201（app-0-88，8h）：**Heap Used 峰值 3.86G / Max 4.29G（≈90%）、Old Gen 多次贴顶、GC 锯齿剧烈**
→ **`OutOfMemoryError` 嫌疑最大**（也可能 `StackOverflowError`）。

### 已排除
- 不是 broker 限流（blocked=0）、不是 CPU 死循环（load 3~5%）、不是 OCR 服务整体挂（同 pod OCR 线程正常）。
- `haveStreamTables`（`parse_stream_table=true` 走的那条）只做纯文本判定、**不发 OCR**；`fallbackOcrPage` 仅在 `is_immediate_ocr=true` 时内联（prod 为 false）。
- `PaddleOcrProcessor` okhttp 三超时 60s、`PaddleOcrClient.callWithTimeout` 60s，OCR 侧有界。

### 待用户协助（确证 Error 类型）
取 **容器 stdout/stderr**（不是应用日志文件）：`kubectl logs <app-0-88 的 pod> --previous` 或 `docker logs`；
JVM 默认 uncaught handler 会打印 `Exception in thread "pulsar-receive-N" java.lang.OutOfMemoryError: ...` + 栈。
下次可加 `-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=...` 抓现场。

### 修复方向（用户要求先只查因；后续已按用户指令实施第 1 步）
1. `consumeReceiveLoop` 把 `catch (Exception)` 扩为 `catch (Throwable)`（Error 也要记日志并**别让线程静默退出**）；
2. 线程退出/异常时 `consumer.close()` 或 `negativeAcknowledge(msg)`/`redeliverUnacknowledgedMessages()`，把槽位与消息交还 broker；
3. 加"消费者线程存活看护"（线程死了自动重建 consumer）；
4. 治本仍要压住堆/递归峰值（大扫描件 + base64 整页图 + 长文档 JSON）。

### 已实施改动（2026-09-14 17:0x）
用户指令："修改代码，catch (Exception) 改为 catch (Throwable"。
- 文件：`java/opendataloader-pdf-server/src/main/java/org/opendataloader/pdf/server/pulsar/PulsarService.java`
- **11 处** `} catch (Exception e) {` → `} catch (Throwable e) {`（`start()`、`consumeReceiveLoop`、`handleReceiveMessage`、
  `consumeOcrReceiveLoop`、`handleOcrReceiveMessage`、`sendResultMessage`、`sendOcrPayload`、`toRelativeObsUrl`、
  `extractPdfBaseName`、`acknowledgeQuietly`、`closeQuietly`）；该模块仅此文件含 `catch (Exception`。
- 类 Javadoc 新增 "Thread-survival safeguard" 段，记录"catch Exception 会让 Error 打死线程→消息永久 unacked→槽位永久丢失"的故障现象，防止后人改回。
- 验证：`read_lints` 0 错误；`cd java/opendataloader-pdf-server; mvn -o -q compile -DskipTests` 退出码 0，
  且 `target/classes/.../PulsarService.class`(16:57:47) 比源码(16:57:30) 新 → 确实重编译通过。`findstr` 复核 11 处 Throwable、0 处 Exception。
- 注：仅改 `catch` 类型是最小止血——Error 后线程仍会继续循环、消息在 `handleReceiveMessage` 末尾照常 ack；
  但"Error 本身"（大扫描件导致的 OOM/SOE）与"槽位看护"未处理，属后续排期。

### 决策（用户问："线程已不会被 Error 打死，后续两项还有必要吗"）
结论：**第 1 项（Error 后 nack/close）不需要、且不建议；第 2 项（线程存活看护）非必需、可选延后；真正该做的是第 4 项（查清/压掉 Error 本身）。**
- 第 1 项不必要：`catch (Throwable)` 后，Error 会被 `handleReceiveMessage` 内部吞掉并**继续走到末尾 `acknowledgeQuietly`** → 消息照样被 ack，**不会漏槽位**。
  且改成 nack 会让"确定性失败的大文件"陷入 `nack→重投→再 Error→再 nack` 毒消息循环；`close()` 更糟（丢整个 consumer）。
  现有契约"失败仍 ack、jsonUrl='' 让下游决策（可用 retry_pdf_parse 重试）"已覆盖。
- 第 2 项非必需：原死因（Error 逃逸循环）已被堵住。仅剩极极端情形（catch 块自身在 OOM 下再抛 Error、JVM 级问题）会退出线程 → 属纵深防御，优先级低。
- 关键副作用（正向）：改动把故障从「**永久停摆（可用性事故）**」降级为「**个别大文件解析失败并正常上报**」，
  且 Error **第一次能被记进日志**（`handleReceiveMessage failed ... <Error> + 栈`）→ 下次可从 Kibana 取到 OOM/SOE 实栈。
- 下一步建议：上线后先**不改代码**，在 Kibana 抓 `handleReceiveMessage failed` 里的 Error 实栈，再决定压堆/流式处理/限并发。
- 另：已被 6 个"死消费者"扣住的 6 条消息不会因本次改动自动恢复，仍需**滚动重启 app-0-88 Pod**。

### 交付物：正式任务记忆文档
- 已在 `docs/memory/` 产出：
  `2026-09-14-Pulsar消费停摆根因-消费线程被未捕获Error打死.md`
  （含：目标、TL;DR、消费模型与 prod-hjs 配置、**观测入口/手法与 3 个踩坑**、**8 步完整定位过程（量化→趋势→定位服务→逐线程最后日志→
  CPU 排除性能型卡住→"缺 success 日志≠卡住"的陷阱→jstack 定案→机制解释）**、根本原因、已排除项、已实施改动与验证、
  "后续两项是否必要"的决策、遗留项、相关文件）。
- 路径变更（用户 2026-09-14 告知）：jstack 现场文件由 `docs/logs/` 移到 **`docs/debuglogs/jstack-app088-20260914_163816.txt`**；
  重命名工具曾把引用改写成丢文件名的 `../debuglogs` / `../../docs/debuglogs`，**已补回完整文件名**（该文档 2 处 + 本文件 1 处），全仓已无 `docs/logs` 残留引用。
