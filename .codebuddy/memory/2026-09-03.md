# 2026-09-03

## 表格单元格背景色（bg_color）修复

文件：`java/opendataloader-pdf-core/.../processors/TableBorderProcessor.java`
方法：`assignBackgroundColorsFromRectangleShapes()`（新增私有方法 `getFillBoxes()`）

背景链路：`DocumentProcessor` → `ShapeRecognizer.recognize()` 生成 `ShapeChunk` →
`TableBorderProcessor.normalizeAndProcessTableBorder()` → `assignBackgroundColorsFromRectangleShapes()`。

### 两个根因

1. veraPDF `TableBorder.getTableBorderCells(IObject)` 通过 `getRowNumber()` **只返回一行**的单元格
   （取纵向重叠最大的行，相等时取最小索引）。跨多行的背景矩形只会给 1 行上色。
2. `ShapeRecognizer.clusterRects()` 的 `areAdjacentOrOverlapping()` 对 x/y 各自用 2pt 容差，
   **只在一个角点相接**的填充矩形也会被合并，union bbox 会覆盖从未填充的区域。

### 修改内容

- 不再调用 `getTableBorderCells()`，改为直接遍历表格全部单元格（跳过 null 与被 span 覆盖的单元格）；
- 重叠计算改用 `ShapeChunk.getComponentBBoxes()`（原始填充矩形），为空时回退 union bbox；
- 只处理 `ShapeChunk.TYPE_RECTANGLE`，不动 `ShapeRecognizer`（用户明确要求）；
- 阈值 `CELL_BACKGROUND_OVERLAP_THRESHOLD = 0.5` 保持。

### 验证样本（docs/pdf/202609021788341792399079538-*.pdf）

- `-2.pdf`：6 行 ×（第4列 503.716–600.094、第6列 707.811–798.52）= 12 格 `#eaeaea`，与内容流 12 处 `0.918 rg` 填充一致。
- `-1.pdf`：5 格（row7 col3/col5、row8 col6、row9 col3/col5）与内容流 5 处填充完全一致；
  修复前 row8 最后一列是误判（该问题在本次修改之前就存在）。

完整排查记录（含从现象到根因的全过程）已写入
`docs/memory/2026-09-03-表格单元格背景色bg_color多行丢失与角点误合并修复.md`。

## 环境备忘

- Maven 本地仓库在 `D:\Maven_Repo`（`settings.xml` 里配置），**不是** `~/.m2/repository`。
- 本机无 `exec-maven-plugin` 之外的方式不便；运行调试入口命令：
  `cd java\opendataloader-pdf-core; mvn -o -DskipTests compile exec:java "-Dexec.mainClass=org.opendataloader.pdf.DebugSample" "-Dexec.classpathScope=runtime" "-Dfile.encoding=UTF-8"`
  （PowerShell 下 `-Dxxx=yyy` 必须加引号，否则被拆分）
- `mvn test` 常被环境判定为耗时较长而跳过，完整单测未跑过。
- 输出目录默认 `tmp_output/`。

## 生产 jar 与本地解析差异排查（202609031788345926201019610.pdf）

- 目标：第3页第2个表格（JSON item id=4, y214-334, lattice_table）第2行。本地解析=1格（x20.7–798.4 跨两列）。
- 复现方法：写出与 DebugSample 完全等价的 `ProdCompare`（tmp_output/prod_run/），用**生产 fat jar 内嵌 core 字节码 + BOOT-INF/lib 依赖**跑同 PDF 同配置 → 结果与本地源码 mvn 运行**完全一致**（第2行同为1格）。
- 生产 jar：`opendataloader-pdf-server-0.0.0.jar`，MD5=3AD6E3537B58A5FDB49B29EABC23F3D2，Built-By=root，Maven 3.6.3，JDK 17.0.15，构建时间 2026-09-03 11:25。jar 内 core 的 DebugSample 仍指向 2023-09-29 旧样本（非当前 HEAD）。
- 类清单：生产 core 与本地 target 同名 252 个 class，逐类字节码因编译/常量池差异全部不同（不可直接比较）。
- 依赖差异：fat jar 内 veraPDF `wcag-algorithms-1.31.33`、`wcag-validation-1.31.99`、`validation-model-1.31.99` 与 D:\Maven_Repo 本地构件 md5 均不同；pdfbox-3.0.4 等相同。
- 运行注意：跑生产 jar 需 JDK17（spring-jcl 为 class v61）；本机 `D:\Applications\Java\jdk-17.0.0.1`。paddle OCR(192.168.1.97:8088) 当前不可达，OCR 页会重试降级但不影响第3页 lattice 表格。
- 结论：给定 jar 与本地源码在该用例上表现一致，生产不一致需向用户澄清（部署 jar 是否一致/产出链路/配置）。
- JDK 交叉验证：生产 jar 内嵌代码在 JDK 11/17/21 下跑同一文件，第3页第2表格行结构完全一致 → JDK 版本差异可排除。用户已确认部署的就是该 jar（MD5 3AD6E353...）。
- 【最终结论】用户确认：生产解析差异是因为该文件走的是另一个 **Python 解析服务**（Pulsar 链路中的 OCR 图片分析/二次合并），并非 opendataloader-pdf-server jar 的问题。Java 侧（本地代码 == 生产 jar）在该用例上行为一致、结果正确，排查关闭。
- 完整排查过程文档（含从问题到根因全记录）已按用户要求写入：
  `docs/memory/2026-09-03-生产jar与本地解析差异排查-定位为Python解析服务.md`
  - 关键证据：第3页第2表格(id=4, bbox 20.4/214.3-798.8/334.5, lattice_table, 5行)。本地源码 vs 生产jar(JK17) vs 生产jar(JK21) 三份 JSON 逐行一致：row0-2 均 ncell=1(x20.7-798.4, col_len=2)，row3/row4 ncell=2。
  - 产物：`tmp_output/prod_run/` 下 `ProdCompare.java`（与 DebugSample 等价的驱动）、`dump_tables.py`（表格结构对比脚本）；三份输出 JSON 在 `tmp_output/{…,prod_out,prod_out_jdk21}/`。

## 协作约定

- 用户要求：**先定位原因，得到明确同意后才改代码**。
- 调试入口 `org.opendataloader.pdf.DebugSample` 由用户自己维护当前指向的 PDF，改动后要恢复原状。
