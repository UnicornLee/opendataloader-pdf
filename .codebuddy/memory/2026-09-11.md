# 2026-09-11

## 图表截图不完整（饼图/柱状图）根因定位 — 未改代码，仅定位

样本：`docs/pdf/354cb7d4-8f79-4429-8768-e3b0e3fcc4e3-{18,28,149}.pdf`（整册 `...-8f79-...pdf` 431 页）。

### 结论（根因）

1. 柱状图（-28 / -149）：
   - `bar_chart` 形状只由**柱子矩形**组成（`ShapeRecognizer.detectBarGroups`），
     截图 bbox = 柱子并集 + `SCREENSHOT_HORIZONTAL_MARGIN=5` / `SCREENSHOT_VERTICAL_TOLERANCE=1`
     （`BarChartProcessor`），增长循环只吸收与当前框在 `COLLECTION_MARGIN=1pt` 内相交的元素。
   - 纵轴刻度数字距柱体左边界 13.7pt（-28）/ 40pt+（-149）、横轴类别文字距柱底 6.1pt、
     图例距柱底 27pt、单位说明在图表上方 → 全部 > 1pt，永远不会被吸收 → 截图里没有。
   - 连**纵轴那条线本身**都没进图：`ShapeRecognizer.groupShapes` 的 `overlapsWithYTolerance`
     要求两框 **x 方向相交**，纵轴在柱体左侧 8.5~13pt（无 x 相交）→ 单独成组，不被并入。
   - 实测 -28 两张图的 bbox 与"柱子并集 ±(5,1)"**完全相等**，证明增长循环一次都没吸收到东西。
2. 饼图（-18）：
   - 饼图（扇形贝塞尔填充路径）**没有任何 shape 类型**：`ShapeRecognizer` 未识别曲线填充图形；
     页面只识别出 11 个图例色块(rectangle) + 4 条引导线(polyline)。
   - 该截图实际由 **FlowchartProcessor** 产生（日志：`Page N: detected flowchart cluster with
     screenshot bbox [314.12, 596.336, 443.56, 637.937]`），命中条件是 `labelsWithConnectors`
     （≥3 文本 + ≥2 连接线），取的是"引导线组 + 1~2pt 内相邻文本"的并集 → 只截到饼图右上角 + 前 2 条图例。
   - 极端脆弱：`MIN_HEIGHT=40`，而该框去掉 ±1pt 后仅 39.60pt，只差 0.4pt 才通过。

### 单页文件 vs 多页文档：同一页结果不同（已实测）

| 输入 | 结果 |
|---|---|
| `...-18.pdf` / 仅第18页 | **完全不产生图片**（24 个文本项） |
| [18, 空白页] / [空白页, 18] | 不产生图片 |
| [17, 18]（2页） | 产生小图 314.12–443.56 × 203.98–245.58 |
| [18, 19, 20]（3页） | 产生小图（同上） |
| [18, 18]（2页） | 产生**巨图** 83.38–512.04 × 42.87–275.39，把标题+两段正文一起吞进图片 |
| 整册 431 页 | 产生小图（同上，imageFile32.png） |

→ 触发与否取决于该页**非图形内容列表**（跨页 pass 会改写：`HeaderFooterProcessor` 把重复的
页眉/页脚内容包成 `SemanticHeaderOrFooter`，标题文本在整册里被当页眉吃掉），
而 `FlowchartProcessor` 的簇判定又处在 40pt 高度阈值边缘 → 1 页/多页结果不同。

### 已确认现象证据

- `-28` 图片 bbox = (174.6,115.9,453.7,247.1)（上）、(175.7,405.3,459.6,526.4)（下），
  纵轴数字 30/25/.../0、横轴 2021…2029E、`单位：万吨` 全部留作文本项。
- `-149` 图片 bbox = (155.5,422.1,483.3,557.5)，纵轴数字 25,000.00…、横轴 2022年度…、
  图例色块+文字、标题 `主要细分类型产品销售额` 全部留作文本项。

### 调试手段备忘
- `tmp_output/` 下脚本：`probe_pdf.py`(fitz 导出矢量/文本)、`compare_streams.py`(内容流逐字节对比，
  已证 -18.pdf 与整册第18页内容流完全一致)、`make_subset.py`/`make_tests.py`(生成子集)、
  `inspect_chart.py`(按 JSON 打印 item+bbox)、`grep_log2.py`(支持 UTF-16 的日志过滤)。
- PowerShell 重定向的日志是 UTF-16，普通 grep/utf-8 读取会失配。
- `DebugSample1.java` 调试后已还原成用户原样（指向 -18.pdf）。

## 柱状图截图区域修复（已实施，用户 5 条口径确认后）

用户口径：① 只到 plot+坐标轴+刻度/类别标签+图例；② 被图片覆盖的文字从文本流移除；
③ 本次只修柱状图（饼图不动）；④ 接受"只吸收短数字标签+细线"的保守策略；⑤ 允许改常量与分组规则。

改动 1：`processors/BarChartProcessor.java`
- 新增 `expandToChartRegion()`，在原有 1pt 增长循环之后：
  1. `isVerticalAxis` / `isHorizontalAxis` 吸附坐标轴：细长(`AXIS_MAX_THICKNESS=6pt`、`AXIS_MIN_LENGTH=20pt`)、
     距 plot 边 ≤ `AXIS_ATTACH_GAP=20pt`、与 plot 重叠率 ≥0.5；
     ⚠ 阈值必须 >3.2pt：-28 下方图表纵轴线宽 3.18pt，3.0pt 时会被漏掉。
  2. `expandToLegend`：`MIN_LEGEND_SWATCHES=2` 个边长 ≤12pt 的近方形色块按 centerY/centerX 对齐成"图例行/列"
     (距 plot ≤`LEGEND_ATTACH_GAP=40pt`)，连同同行的标签文本一起吸收。
  3. `expandToAxisLabels`：刻度标签带 = 纵轴左侧 `TICK_LABEL_BAND=55pt`；类别标签带 = plot 下方
     `CATEGORY_LABEL_BAND=30pt`；过滤条件 `isShortSingleLineText`：单行、高 ≤18pt、≤`MAX_LABEL_CHARS=64` 字符、
     不含句读标点(`[。，、；：！？;!?]`，**不含** ASCII 逗号/句点，否则 25,000.00 会被误杀)。
- 关键坑：标签带必须锚定在**增长循环后的 plot box**（不是被图例扩张后的 box），否则图例会把
  类别标签带"推"到类别标签下方 → 漏掉（-149 会出现）。
- 吸附到的 shape 通过 `markConsumedGroups` 标记，shape 与 text 一并从 pageContents 移除。

改动 2：`processors/FlowchartProcessor.java`
- 新增 `isStillOnPage()` 守卫：组的 shape 已不在 pageContents 时跳过。
  否则 BarChartProcessor 产出的截图会被 FlowchartProcessor 当"流程图"按同一区域再裁一次
  （实测 -28/-149 各产生 1 个孤儿 PNG，最终 bbox 被 Flowchart 版本覆盖）。这也是修复前就存在的隐患。

验证：
- `-28`：2 张图 → (151.9,99.2,455.5,261.4) 含 0~30 刻度+2021…2029E；(139.5,379.9,459.6,542.5) 含 0~250 刻度+2018…2023；
  `单位：万吨/亿元`、`数据来源`、正文仍为文本。
- `-149`：1 张图 → (98.7,393.9,483.3,592.5) 含 25,000.00…、2022/2023/2024年度、图例色块+文字；
  标题 `主要细分类型产品销售额`、`单位：万元` 仍为文本（符合口径①）。
- `-18.pdf`（单页）与 `p18_20.pdf` 饼图输出与改动前逐字节一致（未触碰饼图路径）。
- 单测：`BarChartProcessorTest(6)+CaptionProcessorTest(1)+ConsecutiveImageProcessorTest(6)+
  FlowchartProcessorTest(9)+LineArtProcessorTest(6)` 全绿（28 通过）。注意 `mvn ... surefire:test` 不编译，
  必须先 `compile test-compile`，否则跑的是旧 class（会被误判成失败）。

## 任务记忆文档（按用户要求写入 docs/memory）

文件名（命名规范 `YYYY-MM-DD-任务简单描述.md`）：
`2026-09-11-图表截图区域补全-柱状图坐标轴刻度类别标签图例与Flowchart重复裁剪修复.md`

已写两份：
- `D:\Code\JavaCode\opendataloader-pdf\docs\memory\`（与既有 50+ 篇记忆同目录，格式对齐）
- `D:\Code\JavaCode\opendataloader-pdf-parse\opendataloader-pdf\docs\memory\`（用户指定路径，原先不存在，已创建并复制）

内容含：目标/用户 5 条口径、背景（改动前代码与常量）、**从现象到根因的 10 步定位过程**
（工具建设→排除文件差异→单页/多页反常→子集矩阵→Flowchart 边缘 0.4pt→-28/-149 间隙定量表→
纵轴不入组原因→实施中 4 个实现坑）、根因 A/B/C/D、已实现方案与常量表、验证数据、关键决策、
风险、注意事项（surefire 不编译、PowerShell 日志 UTF-16、-18 单页无图属预期）、相关文件。

## catalog_bookmarks 层级 bug：问题10/问题11 未归入“四、募集资金运用及其他事项”

- 文件：`docs/pdf/354cb7d4-8f79-4429-8768-e3b0e3fcc4e3.pdf`
- 现象：选中源是 `catalog_bookmarks`（JsonWriter 会把选中源从自身 key 删除、合并进 `bookmarks`）。`一/二/三` 能正确把 `问题1-9` 作为子节点，但 `四、` 的 `问题10/问题11` 被错误地放为一级（与 `四、` 同级）。
- 真正根因（已用临时调试打印 + 当前编译代码跑 JSON-only 路径 `extractCatalogBookmarksFromJson` 验证）：
    - `问题N.` 这种前缀在 `BookmarkPrefixClassifier` 中**不被识别**（`classify` 返回 null，prefixType=null）。
    - `assignLevels` → `correctLevelsByPrefix` 第 1 步：把“开头/结尾的无前缀条目强制为 L1”。`目录` 之前的无前缀条目（开头）和 `四、`（最后一个有前缀 #、 的 L1）**之后**的 `问题10/问题11` 因 prefixType=null 被判定为“结尾无前缀”→ 强制 level=1。
    - `问题1-9` 位于 `一/二/三…四、` 之间有前缀的 L1 之间，不是“结尾”，故保留缩进得到的 level=2，正确嵌套。
    - 注：`问题10/11` 的 leftX=113.9 与 `问题1-9` 完全相同（L1 `四、`=89.88），缩进聚类本身正确（gap 24pt>>阈值），是被前缀纠偏步骤错误降级。
- 关键代码：`CatalogBookmarkProcessor.java` 中 `correctLevelsByPrefix`（约 895-913 行 step1）、`clusterByLeftX`、`BookmarkPrefixClassifier.java`（模板无 `问题#.`）。
- 用户最终选择**方向 C**并已落地实现：`correctLevelsByPrefix` 第 1 步改为“仅当无前缀条目位于一级左边界（leftX 与最小 leftX 之差 < 2.0，即无缩进）时才强制 L1；有缩进的（如 `问题10/11`，leftX=113.9 但一级在 89.88）保留缩进聚类得到的层级”。
- 验证：用 `extractCatalogBookmarksFromJson`（纯 JSON 路径，绕过 OCR）跑 `354cb7d4...json`，日志 `extracted 16 bookmarks (5 top-level)`（修复前为 7 top-level），`问题10/问题11` 已正确作为 `四、募集资金运用及其他事项` 的子节点（缩进 2 空格）。lint 0 错误。修复位置：`CatalogBookmarkProcessor.java` `correctLevelsByPrefix` step1（约 895-918 行）。
- 对比：方向 A（给 `问题#.` 注册前缀）更通用但会扩大 `BookmarkPrefixClassifier` 影响面（`isBookmarkCandidate` 等）；方向 C 改动最小、仅修正纠偏步骤，不影响分类器。
- 调试手段：因 OCR(paddleUrl) 在沙箱不可达，改用 `rebuildBookmarks`/`extractCatalogBookmarksFromJson` 纯 JSON 路径 + 临时 DebugCatalog 打印 leftX/level/prefix。

## page_bookmarks 误把正文注释句 "2、…" 当成二级子目录（202609121789114545357081630.pdf）

- 现象：`catalog_bookmarks` 为空 → 选中 `page_bookmarks`；`一、累计诉讼、仲裁事项的基本情况` 只有 1 个子节点且以"2"开头（`2、以上案件涉及金额与最终实际执行金额可能存在一定差异。`），二/三/四 无子节点。
- 数据事实（全文仅 3 页）：page_bookmarks 候选共 5 条 = `#、`/CHINESE 模板 value1-4（一/二/三/四，全为 heading，fs=12.0）+ `#`/ARABIC 模板仅 1 条（page2 id2，`paragraph`，fs=10.45，x0=111.12，以"。"结尾）。其同组 value=1 的 `注：1、…` 因以"注："开头未被 `matchPrefix` 捕获 → 该模板全局只剩孤立 value=2。
- 根因链（`PageBookmarkProcessor`，非缩进机制，是"模板分组+连续编号校验"）：
    1) `matchPrefix`/`hasValidBookmarkSuffix`（:1933/:1945）允许"裸阿拉伯数字+、"，`2、以上…` 成为 `#`/ARABIC 候选；
    2) L1 处 `isValidGroup`（:2031，单值必须为 1，见 :2044）正确判 `#`/ARABIC 无效，L1 spine 正确选 `#、`/CHINESE；
    3) `一、` 的子区间（阅读序 index 1..1）恰好只含该孤立 value=2；L2 的 `selectTemplateForLevel`（:785）允许"局部连续段任意起点"（:811-817）→ 该模板被"复活"；
    4) `cleanCandidatesLocal`(:1095, L2) 三道拦截全失效：句号过滤器需 size>=MIN_CANDIDATES_FOR_PERIOD_FILTER(3)（:100/:1117）此处 size=1 未运行 → `periodFilterApplied=false`；Step4.6"丢弃 value≠1 孤立单链"被 `periodFilterApplied` 门控（:1205-1210）跳过；`isTocLikeGroup` 对 size<2 直接 false（:1296）。
- 位置汇总：collectJsonCandidates:1575/1584、matchPrefix:1933、hasValidBookmarkSuffix:1945、isValidGroup:2031、cleanCandidatesLocal:1095/1117/1188/1205、selectTemplateForLevel:785/811。
- 待定方案（用户要求先不改代码）：A) L2+ 新增"整链均以中文句号结尾且不含 value=1 → 丢弃"（最小、贴合既有"句号=正文"设计）；B) 收集阶段直接拒收 fullText 以"。"结尾的候选（更彻底、有误杀风险）；C) 结构性：某 templateKey 全文无 value=1 则整体丢弃（建议仅限裸数字模板）。
- 待用户澄清：是否接受"以。"作正文判据／是否存在以。结尾的合法标题／规则放收集阶段还是仅 L2+／是否需覆盖"注1、注2 两条都被捕获"的变体。
- **用户最终决定：「value 必须从 1 开始，且严格连续，适用所有模板」**（即选择方案 C 的模板级结构规则，且不限裸数字模板）。
- 已落地实现（`PageBookmarkProcessor.java`）：新增 `discardTemplatesNotStartingAtOne(List<Candidate>)`，在 `buildBookmarksFromCandidates` 排序后、`extractLevel` 之前调用一次；按 `templateKey` 汇总全文 value，凡**从未到达 value=1** 的模板整体丢弃（attachment 哨兵模板不参与）。**不要求全文无缺口**。
- 关键取舍（两次试错得出）：
    1) 先试「每层每父区间都必须从 1 重开」→ 破坏 `testThreeLevelBookmarks`（第2章→第3节）与 `testTopLevelSelectsSingleTemplate`（第2章→二、D），即跨父级续号，属合法结构 → 撤销。
    2) 再试「全文无缺口才有效」→ 破坏 `testLonelyNonOneNotMerged`（`{1,2,5}` 应产出 `[一、A, 二、B]`，缺口由「最宽严格连续链」隔离）→ 撤销。
    3) 最终只要求「必须到达 1」；「严格连续」由既有的链构造（Step2 的 +1 游程 + Step4 仅当 `prev.max+1==group.min` 才拼接）与 L1 的 `trimContiguousSection`/`isValidGroup` 保证——即**输出的编号序列一定严格连续、绝不跳号**，L1 另需从 1 起。
- 验证：`PageBookmarkProcessorTest` 49/49 + `CatalogBookmarkProcessorTest` 3/3 全绿；对 `202609121789114545357081630.json` 跑 `extractPageBookmarksFromJson` 得 `TOP-LEVEL: 4`、无任何子节点（修复前 `一、` 下有伪子节点 `2、以上案件涉及金额…`）。core 模块全量 862 测试中 7 个集成测试类失败（35 fail/16 err），已用「备份→git checkout 还原 HEAD→跑同样 7 类」基线对比：**基线同为 61 run / 35 fail / 16 err，完全一致**，确认是既有环境问题（输出文件不存在、parallel page processing NPE），与本次改动无关。
- 残留局限（未处理，需用户决定）：若注释的 `1、…` 也被识别为候选（本例外因以"注："开头而未被识别），模板将含 {1,2} → 通过模板级校验 → 仍会泄漏为子节点。彻底解决需引入「以。"结尾的正文句」或 heading/字号等文本判据（方案 A/B）。
- 用户补充意见：**「带『注：』的本来就不应该被识别为目录」**。已核实成立：`matchPrefix` 要求文本以编号常量开头，而 `BookmarkConstant` 无任何「注」相关常量 → `注：…` 行永远不会成为候选。
- 由该性质推导出的重要结论（修正上面的"残留局限"）：**内联式**注列表（`注：1、…` 与 `2、…` 同段/同行）的 value=1 永远粘在「注：」上而无法成为候选 → 该模板全文无法到达 1 → 被 `discardTemplatesNotStartingAtOne` 整体丢弃。所以本文件的修复是结构性根治，而非打补丁。
- 语料扫描（tmp_output 下 57 个 JSON，213 个「注」开头段落）：内联式 19 处（会被新规则丢弃，符合预期）；分体式 36 处（「注：」独占/未带编号，后接编号行）。**分体式的 36 处绝大多数其实是真标题或真注列表首项**（如 `58、外币货币性项目`、`54、营业外支出`、`56、库存股`、`2、合并利润表主要数据`、`1、工艺流程`、`（2） 合并成本及商誉`），因此**不能**做「注：之后一律排除」的规则，否则会误删正常目录。真正潜在漏点仅少数（如 `注：` → `1、标的股票数量占比为…`）。
- 结论/建议：不追加文本判据（句号/heading/字号）；如后续确在分体式上观察到实际漏出，再针对性处理。

## 第二例：「最后一个一级目录只有 1 个子节点、且以 2 开头」（202609121789113649042076392.pdf）

- 该文件为股东会通知（6 页，catalog 为空 → 用 page_bookmarks）。输出 8 个一级：一、…六、 + `附件1：` + `附件2：`（后两个是"附件"锚点）。`附件2：` 下只有 1 个子节点 = `2、授权委托书剪报…单位委托须加盖单位公章。`
- 用临时插桩（在 `extractLevel`/`selectTemplateForLevel` 打印 range、组统计、锚点）确认真实链路，查完已移除插桩并恢复源文件（52/52 单测仍绿）。
- **机制一（`附件2：`，用户所问的最后一个一级）**：附件锚点在第 6 页；其后可成为候选的**只有 1 条** —— `2、授权委托书剪报…`（id=11，paragraph）。其 value=1 兄弟是 `注：1、如欲投票同意议案…`（id=10），因以"注："开头**永远不会成为候选**。于是该父区间只剩孤立 value=2：`cleanCandidatesLocal` 的句号过滤器需 size>=3，此处 size=1 → 不运行 → `periodFilterApplied=false` → Step 4.6「丢弃 value≠1 孤立单链」被门控跳过；`isTocLikeGroup` 对 size<2 亦 false → 泄漏。
- **机制二（`附件1：`，同一文件的另一个"只有1个子节点"）**：range=[31,43] 的组统计为 `#`/CHINESE count=3(leftX=112.04)、`（#）`/ARABIC count=3(leftX=104.0)、`#`/ARABIC **count=1**(leftX=90.0)；L2 选择中的"密度优先"（sparsity 越小越优，`selectTemplateForLevel` :846 附近）让 count=1 的退化组胜过两个正常组。该 count=1 源于句号过滤器：区间 7 条中 6 条以"。"结尾（0.857>0.8），唯一非"。"的跨页续行 `2、股东通过互联网投票系统进行网络投票，需按照《深圳证券交易所投资` 被当少数派剔除 → 链 [1,2,3] 断为 [1] 与 [3] → Step 4.6 丢掉 value=3 → 只剩 [1] → 成为唯一子节点。
- 关键认知：上一轮的 `discardTemplatesNotStartingAtOne` 对这两例**都无效** —— `#`/ARABIC 在全文别处确实到达 value=1（它是全文正文编号列表共用模板），模板级校验只能判断"全局"，而缺陷是**局部**的（某父区间的链不从 1 起），且机制二连"局部链"都不是。
- 可选修法（待用户定，均未实施）：1) 把"必须从 1 开始"下沉到每个父区间 → 修机制一，但破坏 `第2章→第3节`、`第2章→二、D`（`testThreeLevelBookmarks`/`testTopLevelSelectsSingleTemplate` 代表的跨父级续号）；2) 续号感知：允许从 v>1 起，但仅当同模板在文档序上紧邻前一个观测值 == v-1（修机制一且保住那 2 个单测）；3) 修 L2 密度偏好（加成员数下限，或改为先比 count 再比密度）→ 修机制二；4) 文本判据（"。"结尾 / 要求 heading）→ 两例都能修，但 `六、备查文件→1.公司第九届董事会第二十九次会议决议`（不以"。"结尾）会保留，且有误杀风险。建议 2+3 组合。

## 【最终决定并已实施】父目录的子目录必须从 1 开始且严格连续

- 用户明确要求：**「我想让父目录的子目录从1开始,且严格连续」** → 选定上述方案 1（父区间级重开），并澄清了此前 "适用所有模板" 的语义是「每个父目录之下」而非「模板全局」。
- 实现（`PageBookmarkProcessor.java` `cleanCandidatesLocal`）：
    - 新增 **Step 4.8**：`chains.removeIf(chain -> chain.isEmpty() || chain.get(0).isEmpty() || chain.get(0).get(0).value != 1);`（放在 Step 4.7 之后，使 value=1 孤儿仍可补全 [2..N] → [1..N]）。链本身由 Step2/Step4 保证严格连续，故「首值==1」即等价于「从 1 开始且严格连续」。
    - 删除被 Step 4.8 完全包含、且门控条件已失效的旧 Step 4.6（"仅当 periodFilterApplied 时丢弃 value≠1 孤立单链"）及其 `periodFilterApplied` 变量（Step 1.5 简化为 `sorted = applyPeriodEndFilter(sorted)`）。
    - 保留上一轮的 `discardTemplatesNotStartingAtOne`（模板级全局校验）；两者层级不同、互不冲突。
    - 同步更新类注释、`selectTemplateForLevel`、`cleanCandidatesLocal` 三处 Javadoc（删除"可跨父目录续号"的旧描述）。
- 单测更新（旧行为已不符合新规格）：`testThreeLevelBookmarks` 与 `testTopLevelSelectsSingleTemplate` 的数据改为「每个父目录从 1 重新编号」（`第2节→第1条 条款三`、`第2章→第1节 分则一般`、`第2章→第1条 条款四`、`二、D→一、D`），使其仍保留原测试意图（三级嵌套 / 顶层单选模板）；另新增 `testChildNumberingMustRestartAtOneUnderEachParent` 显式钉住新规则（`第2章` 下的 `第3节` 必须被丢弃）。
- 验证：`PageBookmarkProcessorTest` **50/50** + `CatalogBookmarkProcessorTest` 3/3 全绿；全量 863 测试 = 35 failures / 16 errors，与改动前基线**完全一致**（同为那 7 个既有环境问题集成测试类）。
- 端到端（纯 JSON 路径）：文档1 `202609121789114545357081630` → 4 个一级、无子节点（保持）；文档2 `202609121789113649042076392` → 8 个一级，**`附件2：` 已无子节点（用户所问的症状已修复）**，但 `附件1：` 仍有 1 个子节点 `1、互联网投票系统开始投票的时间为…` —— 该子节点起值确实为 1，属**另一机制**（L2 的"密度优先"让 count=1 的退化组胜出），需方案 3 才能消除（尚未实施，已告知用户）。

## 句号过滤器改为「双次评估、择优」（用户方案，已实施）

- 事实核查（git）：`MIN_CANDIDATES_FOR_PERIOD_FILTER = 3` 由 `00d2e5d` 引入（与 L2 密度准则同批）；`minorityContainsValueOne` + `periodFilterApplied` 由 `b19b126`（2026-09-10）加入。**两者都只在 `PageBookmarkProcessor`（page_bookmarks）；`CatalogBookmarkProcessor` 里根本没有句号过滤器**（grep 确认，只有无关中文注释）。所以"只改了 catalog"不成立，恰好相反。
- 用户要求：**「句号过滤前选出子目录、过滤后再选出子目录，两个里选一个更合适的」**（即不要单次破坏性地改候选集）。
- 实现（`PageBookmarkProcessor.java`）：
    - `cleanCandidatesLocal` 与 `selectTemplateForLevel` 各加一个 `boolean applyPeriodFilter` 重载（旧签名委托为 `true`，其它调用点如 `cleanedIndicesOf`、`extractChildrenForAnchor` 行为不变）；Step 1.5 门控改为 `if (applyPeriodFilter && level >= 2 && size >= 3)`。
    - `extractLevel`：把「选模板 + 过滤候选 + 清洗 + 映射索引」抽成新助手 `childAnchorIndices(...)`；L2+ 再以 `applyPeriodFilter=false` 跑一遍 `selectTemplateForLevel` + `childAnchorIndices`，由新准则 `preferUnfilteredSelection(unfiltered, filtered)` 择优（若启用过滤结果为空也在此修复，空值检查移到双跑之后）。L1 路径不变（仍用 `cleanCandidates`，且附件 fallback 未动）。
    - **择优准则**：`unfiltered.size() > filtered.size() && filtered.size() <= 1` —— 默认保留「启用过滤」的结果（保住过滤器剔除正文残留的本职），**仅当启用过滤的结果塌缩到 ≤1 个子节点、而禁用过滤能得到更长的序列时才改用后者**（这正是过滤器打断链、留下退化单条的签名）。
- 验证：`PageBookmarkProcessorTest` 50/50 + `CatalogBookmarkProcessorTest` 3/3；全量 863 测试 = 35 failures / 16 errors，与基线完全一致（同 7 个既有环境问题集成测试类）。
- 端到端：文档1 不变（4 一级、无子节点）；文档2 **`附件1：` 现在得到 3 个子节点 `（1）/（2）/（3）`**（原先只有 1 个 `1、互联网投票系统开始投票的时间为…`），`附件2：` 仍无子节点。注：`附件1：` 选中的是 `（#）`/ARABIC 列表（leftX 104.0 在并列时胜过 `#`/CHINESE 的 112.04）；若更希望取 `一．/二./三.` 那组小标题，需要再调 tie-break（未做）。

## L2/L3 并列 tie-break 增加「编号风格层级」先验（用户确认要做，已实施）

- 用户要求：让 `附件1：` 选中 `一．/二./三.` 这组小标题，而不是 `（1）/（2）/（3）`。
- 原因：两者在 font(12.0)/L2密度(3.0)/count(3) 上全部并列，仅由 `leftX` 分出胜负（104.0 < 112.04），而 leftX 平均值被换行的正文行（x0=90 的续行）拉偏，不可靠。
- 实现：新增 `styleDepth(TemplateKey)` —— `第#章/第#节/第#条`、`#`、`#、` → 0；`（#）`、`(#)` → 1；`#）`、`#)` → 2。在 `selectTemplateForLevel` 的 `validGroups.sort` 里插到 **count 之后、leftX 之前**，即「字体 → L2密度 → 条目数 → 风格层级 → 缩进 → 首页 → 首个 topY」。默认分支返回 0。
- 验证：`PageBookmarkProcessorTest` 50/50 + `CatalogBookmarkProcessorTest` 3/3；全量 863 = 35 failures/16 errors，与基线一致。
- 端到端文档2 结果（`附件1：` 变成三级，符合预期）：
  ```
  L1 附件1：
    L2 一．  网络投票的程序
      L3 （1）填报表决意见或选举票数
      L3 （2）股东对总议案进行投票，视为…相同意见。
      L3 （3）对同一议案的投票以第一次有效投票为准。
    L2 二.  通过深交所交易系统投票的程序
    L2 三.  通过深交所互联网投票系统投票的程序
  L1 附件2：   （无子节点）
  ```
  文档1 不变（4 一级、无子节点）。

## 本次工作已归档为正式记忆文档（docs/memory/）

- `docs/memory/2026-09-11-catalog书签问题10问题11未归入上级目录修复.md`（catalog 侧：`correctLevelsByPrefix` Step1 → 方向C）
- `docs/memory/2026-09-11-page-bookmarks子目录从1开始严格连续-含句号过滤双次评估与风格层级tie-break.md`（page 侧：文档①②定位、模板级校验、Step 4.8 父区间级规则、句号过滤双次评估、styleDepth tie-break，含两次试错记录）
